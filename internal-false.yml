version: "3.5"
   
services:
  c1:
    networks:
      external-net:
      default:
    image: makocchi/alpine-dig
    command: top

networks:
  default:
    internal: false
  external-net:
    external: true

